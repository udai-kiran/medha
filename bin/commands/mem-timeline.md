Call mcp__agent-mem__timeline with limit 20. If $ARGUMENTS is non-empty, treat it as a session ID and pass it as the `session` filter. Show each entry as: timestamp, hook type, and a brief excerpt of the content.

Arguments: $ARGUMENTS
