Call mcp__agent-mem__patterns to detect recurring concept and file patterns. If $ARGUMENTS contains "refresh", set detect to true to re-run detection. Display top concepts and files in two short lists.

Arguments: $ARGUMENTS
