Call mcp__agent-mem__remember to persist a new memory. Use the arguments as the `content` field. Infer a short `title` from the content. Set `type` based on what the content is about (prefer: fact, preference, project, feedback). Do not ask for clarification — persist it immediately and confirm with the memory ID.

Arguments: $ARGUMENTS
