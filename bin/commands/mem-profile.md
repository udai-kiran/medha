Call mcp__agent-mem__profile to get the project intelligence snapshot. If $ARGUMENTS is non-empty treat it as the project name, otherwise omit the project field. Display top concepts, top files, memory type distribution, and counts in a readable format.

Arguments: $ARGUMENTS
