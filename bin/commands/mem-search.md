Call mcp__agent-mem__smart-search with the arguments as the `query`. Use mode "hybrid". Show results as a numbered list with title, type, and a one-sentence excerpt. If no results, say so plainly.

Arguments: $ARGUMENTS
