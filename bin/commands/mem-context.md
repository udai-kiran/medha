Call mcp__agent-mem__get-context to assemble injection-ready context. Set includeLongTerm, includeShortTerm, and includeSlots to true. If $ARGUMENTS is non-empty, use it as the `query`. Display the assembled context sections clearly labeled.

Arguments: $ARGUMENTS
