Call mcp__agent-mem__forget to hard-delete a memory. Treat $ARGUMENTS as the memory ID. Set actor to "user" and reason to "user-requested deletion". Confirm the deletion or report any error.

Arguments: $ARGUMENTS
